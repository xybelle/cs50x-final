#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Prompt for input
    int card = get_card();

    // Calculate checksum
    int checksum = calc_check(int sum)

    // Check card length and starting digits

    // 
}
