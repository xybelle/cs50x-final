#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long cc;
    do
    {
        cc = get_long("Card number: ");
    }
    while (cc > 5699999999999999);

    switch(cc)
    {
        case 
    }
}
