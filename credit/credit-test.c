#include <cs50.h>
#include <stdio.h>

long calculate_last(long card);

int main(void)
{
    // Prompt for input
    long card;
    printf("Card number: ");

    // Get numbers to calculate checksum
    long lastnum = calculate_last(card)
}

long calculate_last(long card)
{
    long p;
    p = card

}
