#include <cs50.h>
#include <stdio.h>

bool valid_triangle(int z);

int main(void)
{
    int x[3];
    for (int i = 0, i < 3, i ++)
    {
        x[i] = get_int("Give me a positive number: ");
    }

    int y = valid_triangle(x[]);
    if (y = 0);
    {
        printf("True\n");
    }
    else
    {
        printf("False\n");
    }
}

bool valid_triangle(int z)
{
    int sumoftwosides(x);
    return 0;
}

int sumoftwosides(x[])
{
    if (x[0] < (x[1] + x[2]))
    {
        return 0;
    }
    else
    {
        printf("False\n");
    }
}

