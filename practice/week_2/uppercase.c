#include <cs50.h>
#include <ctype.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string name = get_string("Before: ");
    printf("After:  ");
    for (int i = 0; i < strlen(name); i++)
    {
        if (s[i] >= 'a' && s[i] <= 'z')
        {
            printf("%c", s)
        }
    }
}
